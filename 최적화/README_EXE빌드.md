# 🚀 EXE 빌드 빠른 시작 가이드

이 프로젝트를 단일 EXE 파일로 패키징하는 방법

---

## ⚡ 빠른 시작 (3단계)

### 1단계: 검증

```bash
python3 validate_build.py
```

모든 항목이 ✅이면 다음 단계로!

### 2단계: 빌드

**Windows:**
```cmd
build.bat
```

**Linux/Mac:**
```bash
./build.sh
```

### 3단계: 테스트

```bash
cd dist
./블로그SEO최적화.exe
```

---

## 📦 빌드 결과물

```
dist/
├── 블로그SEO최적화.exe          ← 배포용 실행 파일
└── 금칙어 수정사항 모음.txt      ← 필수 데이터 (함께 배포)
```

---

## 📁 프로젝트 구조

```
blogm/
├── 🎯 실행 파일
│   ├── blog_optimizer_gui.py           ← GUI 메인 프로그램
│   ├── advanced_seo_optimizer.py       ← 고급 최적화 엔진
│   ├── blog_optimizer.py               ← 기본 최적화 엔진
│   └── optimize_txt.py                 ← TXT 파일 최적화
│
├── 🔨 빌드 설정
│   ├── blog_optimizer.spec             ← PyInstaller 설정
│   ├── build.bat                       ← Windows 빌드 스크립트
│   ├── build.sh                        ← Linux/Mac 빌드 스크립트
│   ├── validate_build.py               ← 빌드 전 검증
│   └── requirements.txt                ← Python 패키지 목록
│
├── 📊 데이터
│   └── 금칙어 수정사항 모음.txt        ← 금칙어 데이터 (필수)
│
└── 📚 문서
    ├── README_EXE빌드.md               ← 이 파일
    ├── 빌드가이드.md                   ← 상세 빌드 가이드
    ├── 사용가이드_EXE.md               ← 사용자용 가이드
    ├── TXT_사용가이드.md               ← TXT 모드 가이드
    └── ADVANCED_SEO_GUIDE.md           ← SEO 최적화 가이드
```

---

## 🎯 각 파일 역할

### 실행 파일

| 파일 | 역할 |
|------|------|
| `blog_optimizer_gui.py` | **메인 프로그램** - Tkinter GUI 인터페이스 |
| `advanced_seo_optimizer.py` | 고급 SEO 최적화 엔진 (내용 확장, 키워드 분석) |
| `blog_optimizer.py` | 기본 최적화 엔진 (금칙어 치환, AI 패턴 제거) |
| `optimize_txt.py` | TXT 파일 전용 최적화 (CLI 모드) |
| `optimize_excel_advanced.py` | Excel 파일 전용 최적화 (배치 처리) |

### 빌드 파일

| 파일 | 역할 |
|------|------|
| `blog_optimizer.spec` | PyInstaller 빌드 설정 (의존성, 데이터 파일 등) |
| `build.bat` | Windows 자동 빌드 스크립트 |
| `build.sh` | Linux/Mac 자동 빌드 스크립트 |
| `validate_build.py` | 빌드 전 필수 파일/모듈 검증 |
| `requirements.txt` | Python 패키지 의존성 목록 |

---

## 🔧 사전 요구사항

### 필수

- Python 3.8 이상
- pip (Python 패키지 관리자)

### 설치 명령

```bash
# 필수 패키지 설치
pip install -r requirements.txt

# 빌드 도구 설치
pip install pyinstaller
```

---

## 🎨 커스터마이징

### EXE 파일명 변경

`blog_optimizer.spec` 파일에서:

```python
exe = EXE(
    ...
    name='블로그SEO최적화',  # ← 원하는 이름으로 변경
    ...
)
```

### 콘솔 창 표시

디버깅 시 유용:

```python
exe = EXE(
    ...
    console=True,  # False → True로 변경
    ...
)
```

### 아이콘 추가

```python
exe = EXE(
    ...
    icon='myicon.ico',  # 아이콘 파일 경로
    ...
)
```

---

## 🐛 문제 해결

### 빌드 실패

```bash
# 1. 검증 실행
python3 validate_build.py

# 2. 캐시 정리 후 재시도
rm -rf build dist __pycache__
python3 -m PyInstaller blog_optimizer.spec --clean
```

### 모듈 누락

```bash
# requirements.txt 재설치
pip install -r requirements.txt --force-reinstall
```

### Windows에서 실행 안됨

Visual C++ 재배포 패키지 필요:
https://aka.ms/vs/17/release/vc_redist.x64.exe

---

## 📊 빌드 시간

| 단계 | 예상 시간 |
|------|----------|
| 검증 | 5초 |
| 의존성 분석 | 30초 |
| 바이너리 생성 | 2-5분 |
| 패키징 | 30초 |
| **총** | **3-7분** |

---

## 🎁 배포 패키지 생성

### Windows

```cmd
# 1. 빌드
build.bat

# 2. 배포 폴더 생성
mkdir 블로그SEO최적화_v1.0
copy dist\블로그SEO최적화.exe 블로그SEO최적화_v1.0\
copy "금칙어 수정사항 모음.txt" 블로그SEO최적화_v1.0\
copy 사용가이드_EXE.md 블로그SEO최적화_v1.0\사용가이드.txt

# 3. ZIP 압축
```

### Linux/Mac

```bash
# 1. 빌드
./build.sh

# 2. 배포 패키지 생성
mkdir 블로그SEO최적화_v1.0
cp dist/블로그SEO최적화 블로그SEO최적화_v1.0/
cp "금칙어 수정사항 모음.txt" 블로그SEO최적화_v1.0/
cp 사용가이드_EXE.md 블로그SEO최적화_v1.0/사용가이드.txt

# 3. ZIP 압축
zip -r 블로그SEO최적화_v1.0.zip 블로그SEO최적화_v1.0/
```

---

## 📚 추가 문서

| 문서 | 대상 | 설명 |
|------|------|------|
| `빌드가이드.md` | 개발자 | 상세 빌드 가이드 (문제 해결, 고급 설정) |
| `사용가이드_EXE.md` | 사용자 | EXE 사용법 (배포용) |
| `TXT_사용가이드.md` | 사용자 | TXT 파일 모드 사용법 |
| `ADVANCED_SEO_GUIDE.md` | 사용자 | SEO 최적화 기준 설명 |

---

## ✅ 체크리스트

### 빌드 전

- [ ] Python 3.8+ 설치 확인
- [ ] `pip install -r requirements.txt`
- [ ] `pip install pyinstaller`
- [ ] `python3 validate_build.py` 실행
- [ ] 모든 항목 ✅ 확인

### 빌드

- [ ] `build.bat` (Windows) 또는 `./build.sh` (Linux/Mac) 실행
- [ ] `dist/` 폴더에 실행 파일 생성 확인
- [ ] 금칙어 파일이 같은 폴더에 있는지 확인

### 테스트

- [ ] 실행 파일 더블클릭
- [ ] GUI 정상 실행
- [ ] 파일 브라우저 동작
- [ ] TXT 파일 최적화 테스트
- [ ] Excel 파일 최적화 테스트
- [ ] 결과 파일 생성 확인

### 배포

- [ ] 배포 폴더 생성
- [ ] 실행 파일 + 금칙어 파일 복사
- [ ] 사용가이드 추가
- [ ] ZIP 압축
- [ ] 클린 환경에서 테스트

---

## 🎉 성공!

빌드가 완료되면:

```
dist/블로그SEO최적화.exe  ← 이 파일을 배포하세요!
```

**함께 배포해야 할 파일:**
- `블로그SEO최적화.exe`
- `금칙어 수정사항 모음.txt`
- `사용가이드.txt` (선택)

---

## 💡 팁

### 빠른 재빌드

```bash
# spec 파일만 수정한 경우
pyinstaller blog_optimizer.spec

# Python 코드 수정한 경우
pyinstaller blog_optimizer.spec --clean
```

### 빌드 로그 저장

```bash
# Windows
build.bat > build.log 2>&1

# Linux/Mac
./build.sh 2>&1 | tee build.log
```

### 크기 최적화

```bash
# UPX 압축 사용 (파일 크기 30-50% 감소)
pyinstaller blog_optimizer.spec --upx-dir=/path/to/upx
```

---

**작성:** 2024-01-13
**버전:** 1.0
**문의:** GitHub Issues
